% Incomplete script file used to compute the firm's effective supply curve.

% n = 21; a = 0.5; b = 2.0;
% [basis,Phi,pnodes] = fundefn('cheb',n,a,b);
% 
% p = nodeunif(501,a,b);
% figure; plot(fapprox(c,p),p);
% figure; plot(m*fapprox(c,p),p,p.^(-eta),p);
% figure; plot(p,resid(c,p))