% Incomplete script file used to compute the monoplist's effective supply
% curve.

% n = 21; a = 0.5; b = 2.5;
% [basis,Phi,pnodes] = fundefn('cheb',n,a,b);

% n = 1000;
% p = nodeunif(n,a,b);
% figure; plot(fapprox(c,p),p)
% figure; plot(p,resid(c,p))