% Incomplete script file used to compute the implicit function.

% n = 31; a = 1; b = 5;
% [basis,Phi,xnodes] = fundefn('cheb',n,a,b);

% n = 1000;
% x = nodeunif(n,a,b);
% figure; plot(x,fapprox(c,x))
% figure; plot(x,resid(c,x))