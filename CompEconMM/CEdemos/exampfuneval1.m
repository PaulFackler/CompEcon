% Incomplete script file that illustrates fundefn and funeval in one dimension.

% f  = @(x)  exp(-x);
% d1 = @(x) -exp(-x);
% d2 = @(x)  exp(-x);


% dfit1 = funeval(c,basis,x,1);
% dfit2 = funeval(c,basis,x,2);
% figure; plot(x,dfit1-d1(x));
% figure; plot(x,dfit2-d2(x));